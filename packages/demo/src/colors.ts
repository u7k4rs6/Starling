/**
 * FRONTEND §3: "Two replicas, two colours (amber and teal, or pick two,
 * but only two)." A third replica (the solo pane, opened via the
 * "third replica" button) reuses the amber slot — colour identifies
 * *a* replica in whichever pane it's rendered, not a specific one of
 * three; the point being demonstrated (two is not a special case) never
 * needs three simultaneously-visible colours to make it.
 */
export const REPLICA_A_COLOR = "#d68c27"; // amber
export const REPLICA_B_COLOR = "#2a9d8f"; // teal
